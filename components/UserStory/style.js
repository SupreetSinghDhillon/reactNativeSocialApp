import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  storyContainer: {
    margin: 10,
  },
  storyImage: {
    width: 70,
    height: 70,
    borderRadius: 100,
  },
  firstName: {
    textAlign: 'center',
    marginTop: 5,
    color: 'blue',
  },
});

export default style;
